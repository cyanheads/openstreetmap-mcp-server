---
summary: "Build tooling: run release:github under bun, drop the unused tsx devDependency"
breaking: false
security: false
---

# 0.2.7 — 2026-06-15

## Changed

- **`release:github` script**: runs under `bun run` instead of `tsx`, which was never a declared dependency — the script failed under Bun.

## Removed

- **`tsx` devDependency**: now unused after the `release:github` switch.
